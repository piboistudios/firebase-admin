package firebase_admin.firestore;

/**
	Filter conditions in a `Query.where()` clause are specified using the
	strings '<', '<=', '==', '!=', '>=', '>', 'array-contains', 'in', 'not-in',
	and 'array-contains-any'.
**/
typedef WhereFilterOp = String;